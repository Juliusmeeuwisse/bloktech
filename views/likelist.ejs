<%- include('partials/head'); -%>
<%- include('partials/header'); -%>

<body>
    <% for ( i=0 ; i < liked.length; i++) { %>
    <section class="likeListItem">
    <article class="likeItemText">
        <p class="defaultText" >
            <%= liked[i].firstname %>
        </p>
        <p class="defaultText"> 
            Age:
            <%= liked[i].age %>
        </p>
        <p class="defaultText">
            Favorite genre:
            <%= liked[i].musicGenre %>
        </p>
    </article>
    <article class="likeItemPic">
        <img class="profileimg_list" src=<%= liked[i].profilepicture %>>
    </article>
</section>
        <% } %>
</body>
<footer>
    <%- include('partials/nav'); -%>
</footer>

</html>